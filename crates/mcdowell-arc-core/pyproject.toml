[build-system]
requires = ["maturin>=1.8,<2"]
build-backend = "maturin"

[project]
name = "mcdowell-arc-core"
version = "0.2.0"
description = "Rust acceleration core for McDowell Arc"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
module-name = "mcdowell_arc_core"
